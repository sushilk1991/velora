**Contract:** End state: synthesize the five supplied perspectives into a decision brief for Velora’s next technical stack choice. Constraint in force: I only use findings raised by those perspectives; I did not add new research. Conclusion: the safest first build is **SwiftPM menubar app + long-lived Python MLX sidecar over Unix socket + `parakeet-mlx` primary STT + Qwen3-4B-Instruct-2507 4-bit cleanup + pasteboard/Cmd-V insertion**, while immediately benchmarking Qwen3-ASR and smaller cleanup models because the biggest unresolved tension is quality vs latency.

**CONVERGENCE**

1. **`parakeet-mlx` is the strongest baseline MLX STT engine among the named choices.**  
Supported by proponent, analyst, pragmatist, skeptic, and explorer. The shared reasons: active 2026 release/activity, real streaming API, word-level timestamps, low memory, and enough real-time factor headroom for the `<1s after stop` target. Skeptic still calls it the only viable MLX STT option, despite bus-factor concerns.

2. **Avoid `lightning-whisper-mlx` as primary.**  
Analyst, explorer, skeptic, and pragmatist all reject it as stale or insufficiently supported. Raised evidence includes last meaningful activity/release around 2024, file/batch-style API, no strong streaming story, and unverified speed claims.

3. **`mlx-whisper` is a fallback, not the default.**  
Proponent, analyst, explorer, and pragmatist agree it remains useful for Whisper compatibility, accented speech, or multilingual fallback, but lacks the native low-latency streaming shape of Parakeet.

4. **Swift shell + Python MLX sidecar is the practical MLX path today.**  
Proponent, analyst, explorer, and pragmatist all support a Swift macOS app supervising a Python MLX process over Unix socket or localhost IPC. Skeptic accepts it only if MLX is immovable, but warns distribution/notarization is the core cost.

5. **Raw PCM streaming is the target transport; WAV handoff is only a fallback/MVP shortcut.**  
Proponent, analyst, explorer, and pragmatist converge on 16 kHz mono PCM chunks over socket/stdin/process boundary. Pragmatist uniquely argues batch-on-release WAV/PCM buffering may already satisfy `<1s` and is a cheap first milestone.

6. **Pasteboard + synthesized Cmd-V is the real-world primary insertion path.**  
All lenses that discussed insertion favor pasteboard/Cmd-V as the broadest working method. AXUIElement is useful for selection reads or app-specific fallback, but not a universal insertion primitive.

**TENSION**

1. **Parakeet vs Qwen3-ASR for default STT.**  
Proponent, analyst, pragmatist, and skeptic choose `parakeet-mlx` first because it has the clearest streaming API and speed evidence. Explorer argues Qwen3-ASR may be the better 2026 default on accuracy, citing Soniqo benchmark numbers: Qwen3-ASR 1.7B at 1.32% WER / RTF 0.027 versus Parakeet TDT v3 INT8 at 2.37% WER / RTF 0.009.  
Evidence that would settle it: local M5 Max/macOS 26 benchmark on Velora-style dictation audio measuring WER, finalization latency after stop, memory, and API stability for both `parakeet-mlx` and Qwen3-ASR.

2. **Can Qwen3-4B cleanup hit `<500ms`?**  
Proponent and analyst say it is plausible when warm, prompt-cached, deterministic, and output-capped. Skeptic and pragmatist argue paragraph reformatting often means 100-400 generated tokens, making 4B decode more like 0.7-2s at cited tok/s. Explorer agrees the target is credible only with warm short-output constraints.  
Evidence that would settle it: a local `mlx-lm` benchmark on the exact M5 Max with Velora prompts, measuring TTFT, prompt-cache benefit, output token counts, and formatting accuracy.

3. **Python sidecar proven pattern vs distribution trap.**  
Analyst cites `gabrimatic/local-whisper` as direct evidence of Swift/Python/Unix-socket shipping pattern. Proponent and pragmatist agree Python is the mature MLX API path. Skeptic argues no mainstream consumer dictation app proves Python+MLX distribution, and warns about notarization, signing, TCC identity, crash supervision, and version skew.  
Evidence that would settle it: a signed/notarized Velora prototype with bundled or bootstrapped Python/MLX, tested across clean macOS 26 installs and rebuild/update cycles.

4. **Pure Swift MLX: emerging option or dead end?**  
Explorer and analyst note active Swift MLX audio projects such as `mlx-audio-swift` or `speech-swift`; analyst says Swift streaming exists for some models but not Parakeet in the checked tree. Pragmatist and skeptic emphasize `swift-parakeet-mlx` was archived or not production-ready, and production Swift stacks tend to use CoreML/ANE instead, violating the MLX requirement.  
Evidence that would settle it: a maintained SwiftPM-compatible MLX STT package with streaming Parakeet/Qwen3-ASR, benchmarks near Python MLX, and no full-Xcode requirement.

5. **Hotkey permission strategy.**  
Analyst points to Accessibility + Microphone as the checked pair in local-whisper-style apps, with Input Monitoring possibly involved for CGEventTap. Pragmatist argues `RegisterEventHotKey` may avoid Input Monitoring for v0, while CGEventTap is needed to swallow keys. Skeptic emphasizes support risk around Secure Input and TCC attribution.  
Evidence that would settle it: macOS 26 test matrix for `RegisterEventHotKey`, CGEventTap, Cmd-V posting, Secure Input detection, and TCC prompts in signed SwiftPM builds.

**KEY EVIDENCE**

- `parakeet-mlx` streaming and timestamps: proponent, analyst, explorer, and pragmatist cite the `senstella/parakeet-mlx` README/API showing `transcribe_stream`, chunk ingestion, and token/word timestamps. Source surfaced: https://github.com/senstella/parakeet-mlx

- Parakeet speed/memory: analyst cites issue evidence of ~11 minutes in ~14s on base M1 and ~1.7GB RAM; proponent cites maintainer benchmark around 32.69-39.42 RTFx on M2 Air; pragmatist cites Dictato samples around ~0.50s and very low short-utterance latency. These are not the same benchmark, but all support large latency headroom.

- Qwen3-ASR challenge: explorer cites Soniqo Apple Silicon ASR benchmark showing Qwen3-ASR better WER but slower than Parakeet while still under the 30s/<1s target by RTF arithmetic. Source surfaced: https://soniqo.audio/benchmarks

- Cleanup model: proponent and analyst cite official/MLX benchmark data for `mlx-community/Qwen3-4B-Instruct-2507-4bit`, including about 134 tok/s generation and ~3.35GB memory on M4 Max/macOS 26.1. Explorer and pragmatist cite M5/M5-class variance around ~114-166 tok/s. Claim still needs local measurement for `<500ms`.

- Sidecar architecture: analyst cites `gabrimatic/local-whisper` as near-direct prior art using Unix socket/newline JSON and MLX dictation architecture. Pragmatist cites Python sidecar as mature because `parakeet-mlx` and `mlx-lm` are Python-first. Skeptic flags packaging risk as unresolved.

- Insertion: analyst cites local-whisper source using clipboard save/write/Cmd-V/restore; pragmatist cites VoiceInk `CursorPaster.swift` doing clipboard snapshot, CGEvent Cmd-V, and restore. Skeptic cites VoiceInk/TypeVox-style cascades and Secure Input failures.

- Unsupported or weaker claims: Qwen3-4B “best instruction following” over Gemma/Phi is asserted by proponent/pragmatist/explorer but not backed by a Velora-specific formatting eval. M5 Max `<1s` finalization and `<500ms` cleanup are inferred from published hardware-adjacent numbers, not locally measured.

**OPEN QUESTIONS**

1. Does `parakeet-mlx` beat Qwen3-ASR on Velora’s actual English dictation mix once WER, finalization latency, memory, and streaming stability are measured together?

2. Is `<500ms` cleanup a hard requirement for full paragraph rewriting, or can Velora insert raw text first and apply cleanup asynchronously/optionally?

3. What is the distribution target: developer/open-source users with `uv` bootstrap, signed downloadable `.app`, Homebrew, or Mac App Store-adjacent constraints?

4. Can Swift-only MLX STT become production-ready soon enough to avoid the Python sidecar, specifically with streaming Parakeet or Qwen3-ASR?

5. Which insertion/hotkey path behaves best on macOS 26 under Secure Input, Electron apps, browsers, terminals, and signed rebuilds?

**OPTIONS & NEXT STEPS**

1. **Recommended MVP: Parakeet-first Python sidecar.**  
Stack: SwiftPM menubar, stable signing identity, AVAudioEngine capture, Unix socket to Python, `parakeet-mlx` with `parakeet-tdt-0.6b-v3`, Qwen3-4B cleanup optional, pasteboard/Cmd-V insertion.  
Tradeoff: fastest path to a working MLX app, but accepts Python distribution complexity.  
Smallest next action: build a benchmark harness that records 20-50 utterances and measures Parakeet batch-on-release vs streaming finalization latency.

2. **Accuracy exploration path: Qwen3-ASR bakeoff.**  
Stack: same app/IPC shell, but test Qwen3-ASR 0.6B/1.7B through MLX or `mlx-audio`/related package as candidate default.  
Tradeoff: may improve WER, but API maturity and streaming integration are less settled in the supplied perspectives.  
Smallest next action: run the same local utterance set through Parakeet and Qwen3-ASR, comparing WER and post-stop latency.

3. **Distribution-risk path: defer Python bundling, use `uv` bootstrap.**  
Stack: signed Swift app supervises a `uv`-managed Python 3.12 sidecar and stores models locally.  
Tradeoff: avoids early PyInstaller/notarization work, but first-run setup is less consumer-friendly and may require network.  
Smallest next action: create a clean-machine install script and measure first-run success/failure on macOS 26.

4. **Latency-purist path: no-LLM default, cleanup as mode.**  
Stack: raw Parakeet transcript inserted immediately; Qwen3-4B, smaller Qwen/Llama, or Gemma used only for “polish” mode.  
Tradeoff: protects perceived latency while reducing default formatting quality.  
Smallest next action: run a 20-prompt formatting eval across Qwen3-4B-4bit, Qwen/Llama 1-2B, Gemma 4B, and no-LLM rules, measuring both instruction adherence and wall time.

**Verified**

- I used only the five supplied perspectives as inputs.
- The strongest cross-lens convergence is `parakeet-mlx` primary, Swift/Python sidecar, raw PCM/socket design, Qwen3-4B cleanup candidate, and pasteboard/Cmd-V insertion.
- The main unresolved disputes are Qwen3-ASR vs Parakeet, `<500ms` cleanup feasibility, and Python distribution risk.

**Inferred**

- The recommended default should prioritize Parakeet because four lenses prefer it and the skeptic still names it the only viable MLX STT if MLX is mandatory.
- Qwen3-ASR deserves immediate bakeoff because explorer surfaced concrete benchmark evidence that could overturn the Parakeet default on accuracy.
- The first implementation should keep model choices swappable because the supplied evidence is stronger for architecture than for final model quality on Velora’s actual audio.

---
# Raw perspectives

## proponent (codex)

{'agent': 'codex', 'command': ['codex', '--ask-for-approval', 'never', 'exec', '--skip-git-repo-check', '--ephemeral', '--color', 'never', '-C', '/Users/sushil/Code/Velora', '--sandbox', 'danger-full-access', '--output-last-message', '/var/folders/q2/lz3qfzc151gb6n3q_vh68mf80000gn/T/yoyo-a93etwkl/codex-last-message.txt', '-'], 'exit_code': 0, 'duration_s': 402.707, 'trace_id': '97bd20909d684c4ea9e1ee5893e212d8:1.proponent.codex', 'stdout': 'End state: recommend the strongest pro-MLX stack for Velora, with fallback paths and evidence.\nConstraints in force: investigation only, no file edits; stay PROPONENT; MLX inference required; target STT finalization <1s after ~30s utterance and cleanup <500ms.\nRepo state: `/Users/sushil/Code/Velora` has no implementation yet: `git status --short --branch` returned `## No commits yet on main` and only `.git` / `.claude/settings.local.json` exist.\n\n**Key Findings**\nRecommended stack: SwiftPM menubar app + Python MLX sidecar over a Unix domain socket; `parakeet-mlx` + `mlx-community/parakeet-tdt-0.6b-v3` for STT; `mlx-lm` + `mlx-community/Qwen3-4B-Instruct-2507-4bit` for cleanup; pasteboard+Cmd-V primary insertion with optional AX path.\n\nSTT: `parakeet-mlx` is the best 2026 proponent pick. NVIDIA’s v3 model card says it is 600M params, supports 25 European languages, automatic punctuation/capitalization, word- and segment-level timestamps, and reports Open-ASR avg WER 6.34%. Its HF eval panel reports RTFx 3,332.74. The MLX implementation defaults to `mlx-community/parakeet-tdt-0.6b-v3`, supports word timestamps, and has `transcribe_stream` with 1-second chunk examples. Sources: [NVIDIA v3 card](https://huggingface.co/nvidia/parakeet-tdt-0.6b-v3), [parakeet-mlx README](https://github.com/senstella/parakeet-mlx).\n\nLatency upside is strong. Parakeet-MLX maintainer benchmark on an M2 MacBook Air showed RTFx 32.69-39.42 on several files and summarized “35 seconds of audio for each second.” That implies a hot 30s utterance can finish around ~0.75-0.95s even before projecting to an M5 Max; with streaming, most work happens while the user speaks. Source: [benchmark discussion](https://github.com/senstella/parakeet-mlx/discussions/15).\n\nRepo/package activity favors Parakeet or MLX-Audio over Lightning Whisper. Captured metadata:\n```text\nparakeet-mlx: 953 stars, pushed_at 2026-06-05, PyPI 0.5.2 uploaded 2026-06-05\nmlx-audio: 7506 stars, pushed_at 2026-07-06, PyPI 0.4.4 uploaded 2026-06-06\nlightning-whisper-mlx: 949 stars, pushed_at 2024-05-08, PyPI 0.0.10 uploaded 2024-04-02\n```\nUse `mlx-audio` as fallback/model-zoo/API wrapper, not the primary engine; it exposes STT and an OpenAI-compatible transcription endpoint. Source: [MLX-Audio README](https://github.com/Blaizzy/mlx-audio).\n\nLLM cleanup: use `mlx-community/Qwen3-4B-Instruct-2507-4bit`. Qwen’s card claims improved instruction following/alignment and scores MMLU-Pro 69.6; the MLX benchmark gist reports for the 4-bit model: prompt 1622.27 tok/s, generation 134.52 tok/s, memory 3.35GB on 64GB M4 Max/macOS 26.1. That is enough for <500ms paragraph cleanup if hot, deterministic, and capped to ~60 output tokens. Sources: [Qwen card](https://huggingface.co/Qwen/Qwen3-4B-Instruct-2507), [MLX benchmark gist](https://gist.github.com/awni/c1790e4c3a39be6e8f1c4afd42423d2d), [MLX 4-bit model](https://huggingface.co/mlx-community/Qwen3-4B-Instruct-2507-4bit).\n\nMemory fits comfortably in 36GB unified memory. Parakeet MLX model is 2.47GB on HF; Qwen 4-bit is 2.26GB download / 3.35GB benchmark memory. Even with both hot plus Swift app and audio buffers, the core stack is plausibly under ~8GB working memory.\n\nArchitecture: send raw 16kHz mono PCM frames, not WAV files, for the real-time path. Apple’s AVAudioNode tap is the native capture primitive, and Parakeet’s API accepts chunk arrays via `transcriber.add_audio(chunk)`. Use WAV only for debug/batch fallback. `mlx_lm.server` and `mlx_audio.server` show localhost server patterns are normal in this ecosystem.\n\nPure Swift MLX STT is promising but not the lead path. `mlx-audio-swift` is active and lists STT including Parakeet, but its quickstart is file transcription. The dedicated Swift Parakeet port is archived and says it did not hit desired performance and was resource-intensive. Source: [mlx-audio-swift README](https://github.com/Blaizzy/mlx-audio-swift), [swift-parakeet-mlx README](https://github.com/FluidInference/swift-parakeet-mlx).\n\nText insertion: real dictation apps use pasteboard+synthetic Cmd-V heavily. Psst Free has `ClipboardManager` writing `NSPasteboard.general`, then posting Cmd-V via `CGEvent` keycode `0x09`; Lirevo documents AXUIElement preferred for Cocoa apps and pasteboard fallback for Electron apps. Required permissions: Microphone, Accessibility, and Input Monitoring if using CGEventTap for global push-to-talk. Evidence:\n```text\nPsst README/CLAUDE: ClipboardManager = NSPasteboard + CGEvent Cmd+V paste\nPsst HotkeyManager lines 45-53: AXIsProcessTrusted + CGPreflightListenEventAccess\nLirevo docs lines 100-105: AXUIElement path preferred; pasteboard fallback for VS Code, Cursor, Slack, Discord\n```\n\n**Strongest Point**\nThe upside is unusually clean: Parakeet gives Whisper-class or better English accuracy with native timestamps and enough MLX speed headroom that Velora can spend latency budget on cleanup and insertion. With Parakeet streaming during speech and Qwen hot in memory, success looks like: live partials while speaking, final transcript almost immediately on release, cleanup in a few hundred ms, then one paste event into the foreground app.\n\n**Confidence**\nHigh for `parakeet-mlx` + Qwen3-4B + Swift/Python sidecar as the best first implementation. What would change my view: a current M5 Max benchmark showing Parakeet-MLX streaming finalization consistently >1s for 30s English dictation, or `mlx-audio-swift` proving production-grade Parakeet streaming with lower latency than Python.\n\n**Open Questions**\n1. Does Parakeet v3 handle Velora’s expected proper nouns/jargon better than Whisper large-v3-turbo on real user audio?\n2. Should the first release use pasteboard+Cmd-V only, or include AX insertion for native Cocoa apps from day one?\n3. Should the sidecar protocol be UDS-only for privacy, or localhost HTTP/WebSocket for easier debugging and plugin compatibility?\n\n**Verified**\n- No Velora implementation exists yet in the repo.\n- Parakeet, Qwen, package freshness, benchmark, and insertion claims above trace to captured repo/API/model-card outputs.\n- No files were modified.\n\n**Inferred**\n- M5 Max latency should beat the cited M2 Air/M4 Max figures, but I did not run a local M5 benchmark.\n- <500ms cleanup is achievable when Qwen is already loaded and output is tightly capped; cold-start time is excluded.', 'stderr': '', 'stdout_truncated': False, 'stderr_truncated': False, 'lens': 'proponent'}

## skeptic (claude)

{'agent': 'claude', 'command': ['claude', '-p', '--no-session-persistence', '--permission-mode', 'bypassPermissions'], 'exit_code': 0, 'duration_s': 278.478, 'trace_id': '97bd20909d684c4ea9e1ee5893e212d8:2.skeptic.claude', 'stdout': '# Skeptic report — Velora stack decision\n\n**Contract:** Build the strongest evidence-led case *against* the proposed stack (MLX STT engine + mlx-lm cleanup model + Swift-menubar-with-Python-sidecar + synthetic text insertion), naming failure modes, hidden costs, and prior art that contradicts it. Investigation only; the repo at `/Users/sushil/Code/Velora` is empty (verified: only `.git` and `.claude` exist), so all evidence is from primary web sources.\n\n## Key findings\n\n**1. The MLX requirement itself is the biggest liability — no shipping dictation app uses it.** Every successful app in this exact category is native, not MLX-Python: Superwhisper runs on [whisper.cpp](https://weesperneonflow.ai/en/blog/2026-06-23-whisper-cpp-setup-guide-local-speech-recognition-2026/); [VoiceInk](https://github.com/Beingpax/VoiceInk) (the leading open-source Superwhisper alternative) uses whisper.cpp bindings plus Parakeet via FluidAudio on the Neural Engine ([architecture writeup](https://starlog.is/articles/developer-tools/beingpax-voiceink/)); [WhisperKit hit v1.0.0 in May 2026](https://whipscribe.com/tools/whisperkit) as Argmax\'s production Swift SDK (CoreML/ANE, "no PyTorch, no Python"). Meanwhile macOS 26 ships **SpeechAnalyzer/SpeechTranscriber**, which [MacStories measured at 2.2× faster than MacWhisper\'s large-v3-turbo](https://www.macstories.net/stories/hands-on-how-apples-new-speech-apis-outpace-whisper-for-lightning-fast-transcription/) (34-min file in 45s) with no dependencies at all ([MacRumors](https://www.macrumors.com/2025/06/18/apple-transcription-api-faster-than-whisper/)). MLX also runs on the GPU, not the ANE — worse energy profile for an always-resident background app than the CoreML/ANE path competitors use.\n\n**2. Of the MLX STT options, three of four are disqualified, and the survivor has a bus factor of one.**\n- **mlx-whisper**: batch API only — no streaming; [reported ~10× slower than insanely-fast-whisper on M1 and "not very stable"](https://medium.com/@GenerationAI/streaming-with-whisper-in-mlx-vs-faster-whisper-vs-insanely-fast-whisper-37cebcfc4d27). You\'d hand-build VAD, chunking, and overlap-dedup — the layer where hobby dictation projects die.\n- **lightning-whisper-mlx**: effectively abandoned — [17 total commits, no releases ever published, 15 open issues, 5 unreviewed PRs](https://github.com/mustafaaljadery/lightning-whisper-mlx), last substantive activity ~2024. No streaming, no word timestamps.\n- **mlx-audio**: [68 open issues](https://github.com/Blaizzy/mlx-audio/issues); installs break on Python 3.13 (blis compile failure, [#452](https://github.com/Blaizzy/mlx-audio/issues/452)), broken API signatures ([#425](https://github.com/Blaizzy/mlx-audio/issues/425)), dependency-resolution failures ([#349](https://github.com/Blaizzy/mlx-audio/issues/349)). A kitchen-sink TTS+STT+STS library, not a hardened ASR engine.\n- **parakeet-mlx**: the healthiest ([v0.5.2, released 2026-06-05](https://pypi.org/project/parakeet-mlx/), 953 stars) — but a [single-maintainer project whose own roadmap marks "chunking with continuous context" as only partially achieved](https://github.com/senstella/parakeet-mlx), and the underlying [parakeet-tdt-0.6b-v2 is English-only](https://huggingface.co/nvidia/parakeet-tdt-0.6b-v2) (v3 adds only [25 European languages](https://arxiv.org/abs/2509.14128)). Choosing it caps Velora\'s language roadmap at day one and stakes the core engine on one person\'s hobby repo.\n\n**3. The <500ms LLM cleanup target fails on arithmetic.** Reformatting a paragraph means regenerating it: ~150–400 output tokens. Best published MLX numbers put [Qwen3-4B at ~159 tok/s on Apple Silicon](https://llmcheck.net/benchmarks) (and [Qwen3-14B at ~38 tok/s on M4 Max](https://markaicode.com/benchmarks/hugging-face-qwen-3-m4-max-throughput-benchmark/)); even at 160 tok/s, 250 tokens ≈ 1.6s — 3× over budget before prompt processing. Worse, [Qwen3\'s hybrid thinking mode is on by default](https://huggingface.co/Qwen/Qwen3-4B-MLX-4bit) — forget `enable_thinking=False`/`/no_think` and one paragraph triggers thousands of reasoning tokens ([reasoning costs ~14× tokens, ~6× wall-time](https://unsloth.ai/docs/models/qwen3.5)). The target is achievable only by shrinking the task (rule-based punctuation, diff-only edits, or sub-1B models with worse instruction-following) — meaning the LLM stage as specced doesn\'t survive contact with its own latency budget. It also parks ~2.5–3 GB of wired weights in memory permanently, alongside the STT model, in a background utility.\n\n**4. The Python sidecar is a distribution trap, not a "proven pattern."** The pattern is proven for developer tools and self-hosted servers ([faster-whisper-dictation](https://github.com/bhargavchippada/faster-whisper-dictation) sends audio to a WebSocket server), not for shipped consumer macOS apps. Shipping it means either (a) bundling CPython+MLX+numpy+librosa and notarizing it — PyInstaller\'s `base_library.zip` [breaks notarization](https://github.com/pyinstaller/pyinstaller/issues/5112), hardened runtime [breaks numpy imports](https://groups.google.com/g/pyinstaller/c/csk5zLIwbmw), and every nested dylib must be [signed inside-out](https://haim.dev/posts/2020-08-08-python-macos-app) — or (b) requiring users to `pip/uv install`, which kills mainstream adoption of an open-source app. Add sidecar lifecycle costs: multi-second cold start (Python import + MLX model load), crash/respawn supervision, and version skew between the two halves. The PCM transport itself is a non-issue — 16 kHz mono float32 is ~64 KB/s, trivial over a unix socket; wav-file handoff just adds latency. The pure-Swift MLX escape hatches ([mlx-audio-swift](https://github.com/Blaizzy/mlx-audio-swift), [DePasqualeOrg/mlx-swift-audio](https://github.com/DePasqualeOrg/mlx-swift-audio), [soniqo/speech-swift](https://github.com/soniqo/speech-swift)) exist but are young and not production-proven — the production-ready Swift runtimes (WhisperKit, FluidAudio) are CoreML, i.e. they violate the MLX constraint.\n\n**5. Text insertion: every single method fails somewhere; plan for a cascade and a support queue.** VoiceInk\'s shipped behavior confirms no single method works: it [uses CGEvent typing for short text and paste events for long text, behind Accessibility permission](https://starlog.is/articles/developer-tools/beingpax-voiceink/); TypeVox documents a [clipboard → AX → keystroke cascade](https://typevox.app/blog/dictation-on-mac/). Failure modes: CGEvent typing is slow for 30s utterances and blocked by Secure Input, which [routinely gets *stuck* on, silently disabling the app](https://espanso.org/docs/troubleshooting/secure-input/) (a top support-ticket generator for Espanso); pasteboard+Cmd-V clobbers the user\'s clipboard with racy restore, and some apps intercept paste; `AXUIElement` setValue fails in Electron/web content and replaces the whole field value (breaks undo/cursor position). TCC needs: Microphone + Accessibility (to post CGEvents), plus Input Monitoring if you use an event tap for the hotkey ([HackTricks overview](https://hacktricks.wiki/en/macos-hardening/macos-security-and-privilege-escalation/macos-security-protections/macos-input-monitoring-screen-capture-accessibility.html)). With a Python sidecar, TCC attribution splits across two processes — the "responsible process" for the mic prompt and the event-posting process must be sorted out, and unsigned SwiftPM dev builds (no full Xcode) get their TCC grants invalidated on every rebuild.\n\n## Strongest point from this lens\n\n**The MLX constraint buys nothing measurable and costs the two things that kill projects like this: distribution and maintenance.** On the same M5 Max, macOS 26\'s built-in SpeechAnalyzer is faster than any Whisper variant with zero dependencies, and the production-grade third-party runtimes (WhisperKit 1.0, FluidAudio/Parakeet, whisper.cpp) are all single-process Swift/C++ — which is precisely what every surviving competitor ships. Requiring MLX forces a two-runtime architecture with a notarization-hostile Python bundle, staked on a bus-factor-one repo (parakeet-mlx), to reach performance parity with what the OS gives away. If the MLX requirement is ideological (open-source inference stack, model flexibility), it should be scoped to where it adds unique value — the LLM cleanup stage via mlx-lm, where MLX genuinely is the best local option — not the latency-critical STT path.\n\n## If MLX is immovable (fallback recommendation from this lens)\n\nparakeet-mlx (TDT 0.6b-v2) is the only viable engine — its batch RTF is high enough that a 30s utterance can transcribe in well under 1s, and it\'s the only one with a streaming API at all. Pair with **Qwen3-4B-Instruct-2507 4-bit** (the non-thinking instruct variant; avoids the thinking-mode footgun — identification of this variant is from model-release knowledge, not a fetched source) but relax the cleanup budget to ~1.5–2s or make cleanup optional/async after raw-text insertion. Sidecar: long-lived Python process, unix socket, raw PCM frames, supervised by the Swift app; distribute via `uv`-based first-run bootstrap rather than PyInstaller. Insertion: paste-with-clipboard-restore as primary, CGEvent typing fallback for short text, Secure Input detection with explicit user-facing warning.\n\n## Confidence, and what would change my view\n\n**Moderate-high** on findings 1, 2, 4, 5 (multiple independent sources, shipped-app behavior). **Moderate** on finding 3: the tok/s figures are third-party benchmarks on adjacent hardware, not M5 Max measurements — a 30-minute local benchmark (`mlx_lm.generate` with a 250-token paragraph on the actual machine) would confirm or refute the <500ms infeasibility claim and is the single cheapest de-risking step. My view would change if: (a) M5 Max pushes 4-bit 4B generation past ~400 tok/s; (b) parakeet-mlx gains a second maintainer or org backing; (c) a shipped consumer app is found successfully distributing a bundled Python+MLX sidecar through notarization.\n\n## Open questions this lens raises\n\n1. Why is MLX *required*? If the answer is "open-source + local," whisper.cpp, WhisperKit, and FluidAudio all satisfy it better for STT; the constraint deserves re-litigation before it dictates a two-runtime architecture.\n2. What is the actual distribution story — GitHub release .dmg (notarization of bundled Python), Homebrew (dependency hell acceptable), or "bring your own Python" (niche audience)? This decision constrains the architecture more than any model choice.\n3. Has anyone measured parakeet-mlx\'s `transcribe_stream` accuracy degradation versus batch mode on real dictation audio? Its own docs describe context/depth trade-offs but publish no WER deltas.\n4. How will the app behave when Secure Input is stuck on (Terminal, password managers) — silent failure is the most-reported failure mode in this app category.\n\n**Verified:** empty repo (ls output above); lightning-whisper-mlx staleness (repo metadata: 17 commits, 0 releases, 15 open issues); parakeet-mlx v0.5.2 / 2026-06-05 / Apache-2.0 / single-maintainer (PyPI + repo fetch); mlx-audio issue list (#452, #425, #349, 68 open); SpeechAnalyzer 2.2× benchmark (MacStories/MacRumors); VoiceInk stack and insertion method (repo + architecture writeups); PyInstaller notarization failures (pyinstaller#5112, Apple dev forums); Secure Input stuck behavior (Espanso docs); Qwen3-4B ≈159 tok/s and 14B ≈38 tok/s (llmcheck.net, markaicode).\n**Inferred:** <500ms cleanup infeasibility (arithmetic from cited tok/s, not measured on M5 Max); MLX GPU-vs-ANE energy disadvantage (architectural fact of MLX, no power measurement cited); Qwen3-4B-Instruct-2507 as the non-thinking variant (model-release knowledge, unfetched); TCC attribution complications for a two-process app (from macOS TCC mechanics, not a tested repro).\n', 'stderr': '', 'stdout_truncated': False, 'stderr_truncated': False, 'lens': 'skeptic'}

## analyst (pi)

{'agent': 'pi', 'command': ['pi', '-p', '--no-session', '--mode', 'text', '--tools', 'read,grep,find,ls,bash,edit,write'], 'exit_code': 0, 'duration_s': 463.218, 'trace_id': '97bd20909d684c4ea9e1ee5893e212d8:3.analyst.pi', 'stdout': 'I now have sufficient primary-source evidence. Writing up the analyst findings.\n\n---\n\n## ANALYST lens — first-principles findings for Velora\n\n**Contract (sized to task):** Four-point stack recommendation for an open-source, local-first, macOS-native (Apple Silicon, macOS 26) MLX-based dictation app. End state: a concrete recommended stack (models + libraries + IPC + insertion) with cited primary sources and fallbacks. Constraints binding: must use MLX for inference; target <1s finalize after ~30s utterance, <500ms paragraph reformat. Scope: investigation only — no files were modified. The `/Users/sushil/Code/Velora` repo is empty (`git status`: no commits; only `.claude/settings.local.json`) — all evidence is external/primary-source; nothing existed to search in-repo.\n\n**Honesty caveat:** I did **not** run any model on the target M5 Max — all latency figures below are *derived from published primary-source numbers on M1/M4* and are labeled INFERRED. I verified repo metadata, READMEs, source files, and one official benchmark table by fetching them.\n\n---\n\n### Key findings\n\n#### Q1 — STT engine: parakeet-mlx is the strongest MLX choice for real-time dictation\n\n- **Primary streaming API exists and is purpose-built for dictation.** `senstella/parakeet-mlx` (953★, pushed 2026-06-05, PyPI 0.5.2, Apache-2.0) exposes `model.transcribe_stream(context_size=(256,256))` returning `finalized_tokens` and `draft_tokens` incrementally as audio chunks are added — verified in README "Streaming Transcription" section + a real pyaudio loop posted by a user in issue #22.\n- **Word-level timestamps are native (TDT).** The `AlignedToken` result carries `start/end/duration` per token — verified in README "Timestamp Result". This is the differentiator vs vanilla Whisper, which requires timestamp sampling heuristics. This matters for Velora (cursor positioning, command parsing).\n- **Speed is comfortably inside target — on M1, let alone M5.** Issue #4 (closed): on a **base M1 8GB Air**, 11 minutes of audio transcribed in **~14s → RTF ≈ 0.021 (~47× realtime)**. Scaling to a 30s utterance: ~0.63s compute on the slowest supported Mac (INFERRED). M5 Max will be faster. The <1s finalize target is plausible but I did not measure it on M5 Max.\n- **Memory is small.** Same issue #4 comment: with `chunk_duration=60`, "RAM increasing by ~1.7GB during inference" for the 0.6B model. On 36GB unified memory this is negligible; no OOM risk for 30s utterances.\n- **Default model:** `mlx-community/parakeet-tdt-0.6b-v3` (English v2, 25 EU langs v3) — the parakeet-mlx default (`--model` default, README).\n- **Runners-up / why not:**\n  - `mlx-whisper` (PyPI 0.4.3, last release 2025-08-29) — mature, OpenAI-compatible, but **no native word-level streaming API** and no dedicated finalize/draft token streaming; heavier (turbo/large) and slower per-RTF than Parakeet TDT on Apple Silicon. Use as the **accuracy fallback** for accented/multilingual audio.\n  - `lightning-whisper-mlx` (949★) — **last push 2024-05-08; effectively unmaintained** (2+ years stale). README claims "10× faster than whisper.cpp, 4× MLX whisper" but provides **no benchmark table or methodology** — I could not verify the numbers. Not recommended as primary in 2026.\n  - `Blaizzy/mlx-audio` (7506★, pushed 2026-07-06, PyPI 0.4.4) — the broadest unified STT/TTS/STS library, supporting Whisper, Parakeet, Qwen3-ASR, **Voxtral Realtime (4B streaming)**, Nemotron streaming, Moonshine. Heavier and server-oriented; attractive if Velora later wants TTS/diarization, but for pure dictation parakeet-mlx is leaner and its streaming API is more directly dictation-shaped.\n  - `tsmdt/whisply`, `karolswdev/HoldSpeak` — cross-platform dictation front-ends, not engines; not relevant as the inference engine.\n- **Note on accuracy caveat:** Open issue #46 reports hallucinations on **non-speech signals** (synthetic) with a claimed 25% reduction via custom code — author flags this may not affect normal speech. Flagging as residual risk, not a blocker for speech dictation.\n\n#### Q2 — Local LLM for text cleanup: Qwen3-4B-Instruct-2507 at 4-bit, via mlx-lm\n\n- **Authoritative numbers from the official `ml-explore/mlx-lm` benchmark table** (`mlx_lm/BENCHMARKS.md`, measured on 64GB M4 Max, macOS 26.1, mlx-lm 0.28.2):\n  - `mlx-community/Qwen3-4B-Instruct-2507-4bit`: **1622 tok/s prompt eval, 134.5 tok/s generation, 3.35 GB memory**, MMLU-Pro 60.72.\n  - Same model bf16: 52.5 tok/s gen, 9.02 GB.\n- **Target math (INFERRED from those numbers):** reformatting a paragraph ≈ 80 output tokens at 134 tok/s ≈ **0.6s generation**, plus system+transcript prompt (~300 tok / 1622 ≈ 0.18s). Shorter outputs land under 500ms; the 134 tok/s is the fastest published 4B-class figure in the official table. 3.35 GB fits trivially in 36GB alongside the ~1.7GB STT model.\n- **mlx-lm 0.31.3 supports prompt caching** (README "Long Prompts and Generations → Prompt caching"). For Velora the cleanup **system prompt is constant** → after the first call, repeated formatting calls skip re-evaluating the system prompt, cutting latency further. This is the load-bearing reason to prefer mlx-lm over a stateless Ollama round-trip for the hot path.\n- **Why Qwen3-4B over the others named:** it is the *only* candidate with a captured official MLX benchmark. Llama-3.2-3B / Gemma-3-4b-it / Phi-4-mini are *not* in `BENCHMARKS.md`; their HF model cards I fetched contained **no tok/s tables** (only license text / usage snippets). I therefore **cannot** make a speed claim for them from captured evidence — flagging as UNVERIFIED. Qwen3-4B is also the most recent (2507, July 2025) and the strongest instruction-follower by MMLU-Pro in the captured set. **Fallback:** Qwen3-4B-8bit if 4-bit formatting fidelity degrades (86.9 tok/s gen, 5.25 GB — still fast, MMLU 63.85); or Qwen3-30B-A3B q4 (113 tok/s, 18.2 GB) only if a 36GB Mac needs top quality and can spare memory.\n\n#### Q3 — Architecture: Swift menubar + Python MLX sidecar over a Unix socket is a *proven, shipping* pattern\n\n- **Proof by existence — `gabrimatic/local-whisper`** (pushed 2026-07-05; near-exact Velora twin): macOS menu-bar/overlay dictation, global hotkey, **Parakeet-TDT v3 default engine**, in-process MLX, grammar cleanup, auto-paste, history. Its `cmd_server.py` docstring is explicit and directly on-point:\n  > "Command socket server for programmatic access to Local Whisper. Separate from the IPC socket (which serves the Swift UI). … Socket: `~/.whisper/cmd.sock` (chmod 600). Protocol: newline-delimited JSON."\n  → i.e. **Swift UI ↔ AF_UNIX socket ↔ Python sidecar**, newline-delimited JSON, `chmod 600`. This is exactly the proposed Velora pattern, in production.\n- **Mic → Python process:** the sidecar captures audio in-process (the recorder uses `AVAudioEngine` on iOS per the README; the macOS path feeds PCM into the Python engine). Issue #22 shows the canonical dictation shape: `pyaudio` 1.5s float32 chunks → `mx.array` → `transcriber.add_audio()`. So the **raw-PCM-over-the-process** path (pyaudio/sounddevice feeding `transcribe_stream`) is the demonstrated streaming approach; writing WAV files to disk is used only for the long-form/batch CLI (`--output-dir`), not the hot dictation path.\n- **Pure-Swift MLX STT alternative now exists and is credible:** `Blaizzy/mlx-audio-swift` (692★, pushed 2026-07-02, MIT, SPM, macOS 14+). It implements Parakeet, Whisper, Voxtral Realtime, Qwen3-ASR, Moonshine, etc. in Swift over `ml-explore/mlx-swift`, with a dedicated `Sources/MLXAudioSTT/Streaming/` (IncrementalMelSpectrogram, StreamingEncoder, StreamingInferenceSession) and per-model `NemotronASRStreamSession` / `VoxtralRealtimeStreamSession`. **Caveat (verified by tree listing):** there is **no `ParakeetStreamSession.swift`** — Swift *streaming* is wired for Nemotron and Voxtral Realtime, not yet Parakeet (Parakeet Swift is batch `model.generate(audio:)` per the README). So a pure-Swift stack would currently stream via Voxtral Realtime (4B, heavier) or Nemotron, **not** Parakeet. This is the key trade-off: pure-Swift eliminates the Python sidecar and IPC entirely, at the cost of a heavier/different streaming model and a younger, less battle-tested surface than parakeet-mlx.\n\n#### Q4 — Text insertion: clipboard + Cmd-V (System Events keystroke) is what shipping dictation apps use; Accessibility API is used to *read* the selection; CGEventTap drives the global hotkey\n\n- **Insertion (verified in `local-whisper/app_pipeline.py::_paste_text_at_cursor_inner`):** save clipboard via `pbpaste` → write transcript via `pbcopy` → `osascript -e \'tell application "System Events" to keystroke "v" using command down\'` → sleep → restore clipboard. This is the **pasteboard + Cmd-V** pattern (System Events synthesizes the Cmd+V keystroke, which is CGEvent under the hood). It is the dominant real-world approach precisely because `AXUIElement.setValue` only works for a subset of text fields (it fails in many native and most Electron controls).\n- **Reading the user\'s selection (verified in `shortcuts.py`):** `ApplicationServices.AXUIElementCopyAttributeValue` with `kAXFocusedUIElementAttribute` and `kAXSelectedTextAttribute` — so the Accessibility API is used for *input* (selected-text transforms), while paste is used for *output*. This split is the pattern to copy.\n- **Global hotkey (verified in `key_interceptor.py`):** `Quartz.CGEventTapCreate` with `kCGSessionEventTap`, `kCGHeadInsertEventTap`, suppressing by returning `None`. This is the only reliable way to swallow a global shortcut before it reaches the frontmost app.\n- **TCC permissions (verified in `cli/doctor.py`):** the app explicitly checks **Accessibility** (`check_accessibility_trusted`) and **Microphone** (`check_microphone_permission`), pointing users to System Settings → Privacy & Security. So the required set is **{Microphone, Accessibility}**; Input Monitoring is the alternate/overlapping TCC bucket CGEventTap may be granted under (Quartz exposes `CGPreflightListenEventAccess`/`CGRequestListenEventAccess` for it). Accessibility is the single permission that covers both the keystroke synthesis and AX selection reads used here.\n- **Packaging caveat captured from source:** doctor.py warns that "macOS TCC identifies the service by the binary path" and "every Python upgrade resets Accessibility/Microphone" for Homebrew Python — a real, documented friction point for a Python-sidecar distribution. This argues for bundling a stable Python (or the pure-Swift path) for Velora.\n\n---\n\n### Recommended stack (concrete, with fallbacks)\n\n| Layer | Primary | Fallback | Evidence |\n|---|---|---|---|\n| STT engine | `parakeet-mlx` 0.5.2, `mlx-community/parakeet-tdt-0.6b-v3`, `transcribe_stream()` | `mlx-whisper` large-v3-turbo for accented/multilingual; `mlx-audio` if TTS/diarization needed later | README streaming API; issue #4 (RTF 0.021 on M1) |\n| Cleanup LLM | `mlx-lm` 0.31.3 + `mlx-community/Qwen3-4B-Instruct-2507-4bit` (enable prompt cache) | Qwen3-4B-8bit if 4-bit quality drops; Qwen3-30B-A3B-4bit for max quality (18 GB) | `mlx_lm/BENCHMARKS.md` (134 tok/s, 3.35 GB, M4 Max) |\n| App shell + IPC | Swift menu-bar (SwiftPM, no Xcode) ↔ Python sidecar over **AF_UNIX socket**, newline-delimited JSON, chmod 600 (path e.g. `~/.velora/sock`) | pure-Swift via `mlx-audio-swift` (drops the sidecar; streams via Voxtral Realtime/Nemotron, not Parakeet) | `local-whisper/cmd_server.py` docstring; `mlx-audio-swift` tree |\n| Mic → engine | raw 16 kHz float32 PCM chunks (sounddevice/pyaudio) → `transcriber.add_audio()` | WAV file for long-form/batch only | parakeet-mlx issue #22 code |\n| Text output | clipboard + Cmd-V via `osascript … keystroke "v" using command down`, save/restore prior clipboard | `AXUIElement.setValue` per-field where supported | `local-whisper/app_pipeline.py` |\n| Selection read | Accessibility API (`AXUIElementCopyAttributeValue`, `kAXSelectedTextAttribute`) | clipboard Cmd+C fallback | `local-whisper/shortcuts.py` |\n| Global hotkey | `CGEventTapCreate(kCGSessionEventTap)` | none (must have a tap) | `local-whisper/key_interceptor.py` |\n| TCC perms | **Microphone + Accessibility** (Input Monitoring may also be prompted by the tap) | — | `local-whisper/cli/doctor.py` |\n\n---\n\n### Strongest point from this lens\nThe entire proposed architecture is **not theoretical**: `gabrimatic/local-whisper` is a shipping, MIT-adjacent, Apple-Silicon dictation app pushed **three days ago** that already uses Parakeet-TDT-v3-as-default + in-process MLX + a Unix-socket sidecar + CGEventTap hotkey + clipboard/Cmd-V paste + the exact Microphone/Accessibility TCC pair. Velora\'s stack is a *validated* pattern, not a bet. Combined with the official mlx-lm benchmark (Qwen3-4B-4bit at 134 tok/s, 3.35 GB), both latency targets have captured numbers behind them rather than estimates.\n\n### Confidence and what would change my view\n- **High confidence** on: existence/maturity of parakeet-mlx streaming, Qwen3-4B-4bit speed/memory, the sidecar+socket+CGEventTap+paste pattern, and the TCC permission set — all from captured primary sources.\n- **Medium confidence** on: hitting **<1s finalize on M5 Max specifically** — I extrapolated from M1/M4 Max numbers and did *not* run on M5 Max (no model download/run performed). The streaming finalize cost (encoder recompute on context window) is the part I least trust without measurement.\n- **What would change my view:** (1) A measured finalize latency on M5 Max >1s for 30s utterances → switch to a smaller Parakeet variant or stream shorter chunks; (2) Qwen3-4B-4bit failing to follow Velora\'s formatting prompts in practice → step up to 8bit or Phi-4-mini (but I have **no captured** MLX speed for Phi-4-mini, so that\'s currently UNVERIFIED); (3) discovery that `mlx-audio-swift` Parakeet streaming ships soon → reconsider dropping the Python sidecar entirely for a cleaner pure-Swift app.\n\n### Open questions this lens raises\n- **No captured head-to-head WER on English** between parakeet-tdt-0.6b-v3 and whisper-large-v3-turbo *on MLX*. The accuracy ranking rests on NVIDIA\'s published NeMo numbers, not an MLX-specific eval. Worth a small LibriSpeech/cv-rnd WER run before committing.\n- **Swift Parakeet streaming:** `mlx-audio-swift` has Parakeet (batch) but no `ParakeetStreamSession.swift` yet — is streaming-Parakeet-in-Swift on the roadmap? If yes, the pure-Swift path becomes strictly attractive and removes the TCC-identity-on-Python-upgrade pain.\n- **Finalize-vs-draft latency trade-off:** `transcribe_stream` exposes both; what\'s the latency from "user stops speaking" to *finalized* (stable) text, distinct from draft? Draft tokens may reflow — Velora must decide whether to insert draft or wait for finalize.\n- **Prompt-cache hit rate** for the cleanup LLM across mixed transcript lengths — quantify the actual latency reduction, since the <500ms paragraph claim leans on it.\n- **Input Monitoring vs Accessibility:** which TCC bucket does macOS 26 actually prompt for `kCGSessionEventTap` (passive listen) vs the Cmd-V keystroke synthesis (active post)? doctor.py only checks Accessibility; confirm Input Monitoring isn\'t additionally required on 26.\n\n**Verified (proof attached):** parakeet-mlx streaming API + word timestamps (README); RTF≈0.021 on M1 + 1.7GB RAM (issue #4 + comments); mlx-audio-swift STT model set + streaming infra + absence of ParakeetStreamSession (repo tree); mlx-lm Qwen3-4B-4bit 134 tok/s / 3.35GB / M4 Max (BENCHMARKS.md); sidecar Unix-socket + newline-JSON pattern (cmd_server.py docstring); clipboard+Cmd-V paste + AX selection read + CGEventTap hotkey + Microphone/Accessibility TCC (app_pipeline.py, shortcuts.py, key_interceptor.py, doctor.

## explorer (codex)

{'agent': 'codex', 'command': ['codex', '--ask-for-approval', 'never', 'exec', '--skip-git-repo-check', '--ephemeral', '--color', 'never', '-C', '/Users/sushil/Code/Velora', '--sandbox', 'danger-full-access', '--output-last-message', '/var/folders/q2/lz3qfzc151gb6n3q_vh68mf80000gn/T/yoyo-2pv8k9xf/codex-last-message.txt', '-'], 'exit_code': 0, 'duration_s': 757.637, 'trace_id': '97bd20909d684c4ea9e1ee5893e212d8:4.explorer.codex', 'stdout': "**Contract**  \nEnd state: a concrete Velora stack recommendation, from the Explorer lens, with alternatives and tradeoffs.  \nConstraints in force: investigation only, MLX required, no invented facts, evidence-backed claims.  \nWhat exists locally: Velora has no committed app code yet; `git log` returned `fatal: your current branch 'main' does not have any commits yet`, and `find` found only `.claude/` plus `.git/`.\n\n**Key Findings**\n\n**Recommended stack:** SwiftPM macOS menubar app + persistent Python MLX sidecar over a Unix domain socket. Stream raw 16 kHz mono PCM chunks to Python. Use `mlx-qwen3-asr` as the default ASR, `parakeet-mlx` as the speed fallback, `mlx-lm` with Qwen3-4B-Instruct-2507-4bit for cleanup, and pasteboard + synthetic Cmd-V for insertion.\n\n1. **Best ASR is likely not Whisper-family: use Qwen3-ASR first, Parakeet second.**  \n   Evidence: Soniqo’s Apple Silicon ASR benchmark reports Qwen3-ASR 1.7B MLX 5-bit at **1.32% WER, RTF 0.027, 36.4x real-time, 1.92 GB RSS**; Qwen3-ASR 0.6B 5-bit at **1.74% WER, RTF 0.014, 70.5x, 1.03 GB**; Parakeet TDT v3 INT8 at **2.37% WER, RTF 0.009, 117.4x, 0.9 GB**. For a 30s utterance, that implies about **0.82s** ASR time for Qwen 1.7B, **0.43s** for Qwen 0.6B, and **0.26s** for Parakeet, before endpointing/cleanup overhead. Source: https://soniqo.audio/benchmarks  \n   Tradeoff: Qwen wins accuracy and still fits the <1s target; Parakeet is faster and simpler for English/European languages but gives away accuracy in that benchmark.\n\n2. **Among the originally named STT engines, `parakeet-mlx` is the best real-time dictation candidate.**  \n   Evidence: `parakeet-mlx` has an explicit streaming API, word/token timestamps, chunking, and a current PyPI release `0.5.2` uploaded 2026-06-05. Its README shows `transcribe_stream(...)`, `add_audio(chunk)`, `result.sentences`, and token `start/end/duration`. Repo activity check showed `senstella/parakeet-mlx` pushed 2026-06-05. Source: https://github.com/senstella/parakeet-mlx  \n   Tradeoff: it is optimized for throughput and streaming, but Qwen3-ASR appears better on English accuracy.\n\n3. **Do not make `mlx-whisper` or `lightning-whisper-mlx` the default.**  \n   Evidence: `mlx-whisper` supports Python API and word timestamps, but it is file/batch oriented; open issues report `word_timestamps=True` memory growth and slow timestamp/clip behavior. Source: https://github.com/ml-explore/mlx-examples/blob/main/whisper/README.md  \n   `lightning-whisper-mlx` claims “10x faster than Whisper CPP” and “4x faster than current MLX Whisper,” but repo push was 2024-05-08 and PyPI upload was 2024-04-02; its public wrapper exposes `transcribe(audio_path, language=None)`, not a streaming dictation API. Source: https://github.com/mustafaaljadery/lightning-whisper-mlx  \n   Tradeoff: Whisper-family models are familiar and robust, but the prior art points to worse fit for low-latency continuous dictation.\n\n4. **`mlx-audio` is useful as an exploration layer, not the first production dependency.**  \n   Evidence: `Blaizzy/mlx-audio` is very active, with repo push 2026-07-06 and broad STT/TTS model coverage. Source: https://github.com/Blaizzy/mlx-audio  \n   Tradeoff: adopters get many models quickly, but Velora’s first production path should depend directly on the winning ASR package to reduce surface area.\n\n5. **Cleanup LLM: start with Qwen3-4B-Instruct-2507-4bit via `mlx-lm`; keep Gemma 3 4B as the strict-format fallback.**  \n   Evidence: Qwen3-4B-Instruct-2507 is Apache-2.0, non-thinking-only, and its model card emphasizes improved instruction following, writing, and alignment; the MLX community 4-bit repo documents direct `mlx_lm.chat`, `mlx_lm.server`, and Python `load/generate` use. Sources: https://huggingface.co/Qwen/Qwen3-4B-Instruct-2507 and https://huggingface.co/mlx-community/Qwen3-4B-Instruct-2507-4bit  \n   Speed caveat: oMLX M5 Max 36 GB benchmark for Qwen3-4B-Instruct-2507-4bit-DWQ showed **114.2 tok/s, TTFT 948 ms, peak 3.6 GB** at 4k context, while another plain Qwen3-4B 4-bit M5 Max run showed **166.8 tok/s, TTFT 259 ms, peak 2.9 GB**. Sources: https://omlx.ai/benchmarks/23p4wy83 and https://omlx.ai/benchmarks/1r03kbyz  \n   Tradeoff: Qwen is the better cleanup default, but the <500ms target is only credible with a warm model, short prompt, and capped output. Gemma 3 4B has stronger IFEval evidence, **90.2** in Google’s card, and oMLX 4-bit runs around **148-158 tok/s**, so it is worth testing for rigid formatting. Source: https://ai.google.dev/gemma/docs/core/model_card_3\n\n6. **Swift app + Python MLX sidecar is the proven path; pure Swift MLX is emerging, not yet the safest default.**  \n   Evidence: prior local dictation projects use persistent daemons and sockets: `pm99lvl/whisper-dictation` keeps a Python Whisper daemon loaded and communicates over `/tmp/whisper_daemon.sock`. Source: https://github.com/pm99lvl/whisper-dictation  \n   Pure Swift alternatives exist: `speech-swift` exposes MLX/CoreML ASR products including `Qwen3ASR`, `ParakeetASR`, and `ParakeetStreamingASR`, with SwiftPM examples. Source: https://github.com/soniqo/speech-swift  \n   Tradeoff: Swift-only may simplify packaging later, but Python has the more mature MLX ASR APIs today.\n\n7. **Mic streaming should be raw PCM over socket, not temp WAV files.**  \n   Evidence: Apple’s AVAudioEngine tap API is designed to record/monitor audio buffers from an audio node. Source: https://developer.apple.com/documentation/avfaudio/avaudionode/installtap%28onbus%3Abuffersize%3Aformat%3Ablock%3A%29  \n   Existing low-latency Parakeet examples consume raw audio chunks rather than waiting on files. Source: https://modal.com/docs/examples/streaming_parakeet  \n   Tradeoff: raw PCM protocol is more engineering work than writing WAVs, but avoids file I/O and supports partials/VAD.\n\n8. **Text insertion: use pasteboard + synthetic Cmd-V first; treat AXUIElement as fallback.**  \n   Evidence: `gnlcsvn/local-whisper` saves the pasteboard, writes recognized text, posts Cmd-V using Quartz CGEvent, then restores clipboard. Source: https://github.com/gnlcsvn/local-whisper/blob/main/inserter.py  \n   `JoshPetrenko/WhisperDictation` documents Accessibility for clipboard + Cmd-V, Input Monitoring for global hold-to-talk via CGEventTap, and Microphone permission. Source: https://github.com/JoshPetrenko/WhisperDictation  \n   Apple requires `NSMicrophoneUsageDescription` for microphone access, and AXUIElement is the accessibility API for controlling other apps. Sources: https://developer.apple.com/documentation/bundleresources/information-property-list/nsmicrophoneusagedescription and https://developer.apple.com/documentation/applicationservices/axuielement_h  \n   Tradeoff: paste + Cmd-V works broadly but needs Accessibility and has clipboard UX risk. AX `setValue` is cleaner where it works, but app compatibility is uneven.\n\n**Strongest Point From This Lens**\n\nThe landscape says Velora should not frame the choice as “which Whisper port?” The stronger 2026 local-first path is **Qwen3-ASR for quality, Parakeet for speed**, with Whisper as compatibility fallback. That gives better latency/memory headroom on 36 GB unified memory and leaves room for a warm 4-bit cleanup LLM.\n\n**Confidence**\n\nMedium-high for the recommended ASR/architecture direction. The evidence is strong enough to choose prototypes, but I did not run end-to-end benchmarks on this exact M5 Max/macOS 26 machine. My view would change if local tests show Qwen3-ASR streaming finalization misses the <1s post-stop target, or if distribution requirements force pure Swift/no Python.\n\n**Open Questions**\n\n- Is Velora targeting direct distribution only, or Mac App Store? Accessibility-based insertion can affect review strategy.  \n- Does the UX require true live partial captions, or only fast final transcript after release/end-of-speech?  \n- Should cleanup run on every utterance, or only when a user enables “polish mode”? That determines whether <500ms LLM latency is hard or optional.  \n- Should the first release support non-English dictation? If yes, Qwen3-ASR gains weight over Parakeet.\n\n**Verified**\n\n- Local repo is effectively empty; no app code was found to constrain the design.  \n- Upstream activity, PyPI versions, benchmark numbers, APIs, and permission requirements were checked against the linked sources above.  \n- No files were modified.\n\n**Inferred**\n\n- M5 Max should meet or exceed the cited M5 Pro/M4 Pro ASR latency, but that is not a substitute for a local Velora benchmark.  \n- The <500ms cleanup target is plausible only with a warm small model and short outputs; current public Qwen3-4B MLX numbers are variable enough that Velora should benchmark Qwen, Gemma, and Llama locally before locking the cleanup model.", 'stderr': '', 'stdout_truncated': False, 'stderr_truncated': False, 'lens': 'explorer'}

## pragmatist (claude)

{'agent': 'claude', 'command': ['claude', '-p', '--no-session-persistence', '--permission-mode', 'bypassPermissions'], 'exit_code': 0, 'duration_s': 497.167, 'trace_id': '97bd20909d684c4ea9e1ee5893e212d8:5.pragmatist.claude', 'stdout': 'All four questions now have evidence. Writing up the pragmatist report.\n\n---\n\n**Contract:** Deliver an execution-focused recommendation for Velora\'s stack (MLX STT engine, cleanup LLM, Swift↔Python architecture, text insertion) with concrete steps, effort estimates, and reversible-vs-irreversible calls, each claim traced to a source. Constraint in force: MLX inference is required. Repo checked: `/Users/sushil/Code/Velora` is empty except `.claude/` — greenfield, nothing to preserve. Investigation only; no files modified.\n\n## Recommended stack (the pragmatist\'s answer up front)\n\n| Layer | Pick | Fallback |\n|---|---|---|\n| STT | **parakeet-mlx** (`mlx-community/parakeet-tdt-0.6b-v3`) | mlx-whisper (large-v3-turbo) for accented/technical speech |\n| Cleanup LLM | **Qwen3-4B-Instruct-2507, 4-bit** via mlx-lm | Llama-3.2-1B/Qwen3-1.7B fast path; "no LLM" verbatim mode |\n| Shell | Swift 6 SwiftPM menubar executable (NSStatusItem, hand-built .app bundle) | — |\n| Sidecar | Long-lived Python 3.12 process (uv-managed venv), models resident | — |\n| IPC | Newline-delimited JSON control + raw PCM frames over a **unix domain socket** | temp-wav handoff for v0 (still meets latency target) |\n| Insertion | Pasteboard + CGEvent Cmd-V with clipboard snapshot/restore | AppleScript `keystroke` variant; AX setValue not recommended as primary |\n\n**The cheap reversible first move:** a batch-on-release pipeline — Swift buffers PCM while the hotkey is held, ships the whole utterance to the sidecar at release, pastes the result. Because Parakeet TDT transcribes ~30s of audio in roughly 0.5s on Apple Silicon, this hits the <1s target *without streaming*. Streaming (`transcribe_stream`), the LLM cleanup stage, and per-app modes are all additive later. **The expensive semi-irreversible moves** are (a) the app-shell language (Swift shell + Python sidecar — reversing to pure-Swift MLX later means rewriting inference glue) and (b) the license (Velora is open-source; copying any code from VoiceInk/MacParakeet drags in GPL-3.0 — decide MIT/Apache-2.0 vs GPL before borrowing a single file). Model choices are fully reversible — they\'re just weight swaps behind the same API.\n\n## Key findings\n\n**1. STT engine — parakeet-mlx wins on latency and API fit; mlx-whisper is the accuracy fallback; the others are out.**\n- [Dictato\'s Mac benchmark](https://dicta.to/blog/whisper-vs-parakeet-vs-apple-speech-engine/): parakeet-mlx averaged **~0.50s per sample vs mlx-whisper\'s ~1.02s** (≈2× faster); qualitative Parakeet latency on Apple Silicon cited at ~80ms for short utterances, "below the threshold of human perception." Whisper implementations land at 200–500ms depending on chip.\n- [senstella/parakeet-mlx](https://github.com/senstella/parakeet-mlx) is Apache-2.0, actively developed (102 commits, v0.5.1 released Feb 2026 per PyPI), installs via `uv add parakeet-mlx`, and — critically — has a real **`transcribe_stream` API** with configurable attention context plus **word-level timestamps via `AlignedToken`** (start/end/duration per token). mlx-whisper has word timestamps but no native streaming; you\'d hand-roll chunking.\n- [lightning-whisper-mlx](https://pypi.org/project/lightning-whisper-mlx/) is **abandoned** (last release April 2024) — eliminate. [mlx-audio](https://github.com/Blaizzy/mlx-audio) is an active umbrella (Whisper, Parakeet, Qwen3-ASR, Voxtral) — fine as a later multi-engine abstraction, but it\'s a heavier dependency than you need for v1.\n- Memory is a non-issue: parakeet-tdt-0.6b is ~1.2–2.5GB resident; whisper-large-v3-turbo 4-bit ~1.5GB; both trivial on 36GB (verified locally: `hw.memsize` = 38.6GB, M5 Max).\n- Honest caveat from the same benchmark family: [FluidAudio\'s CoreML Parakeet](https://dicta.to/blog/speech-to-text-engine-comparison-mac-2026/) beat parakeet-mlx on both speed (~0.19s) and WER (2.5% vs 6.3% in their internal test) by using the Neural Engine. The MLX requirement forgoes that — worth knowing, not worth fighting.\n\n**2. Cleanup LLM — Qwen3-4B-Instruct-2507 4-bit, but the <500ms target only holds for short outputs or with a smaller model.**\n- [LLMCheck\'s Apple Silicon benchmarks](https://llmcheck.net/benchmarks): 4B-class models decode at **~135–148 tok/s on M5 Max** (Qwen 4B ~135, Phi-5 mini ~145) and ~118–125 tok/s on M4 Pro, MLX 4-bit. Reformatting a paragraph (~100–150 output tokens) is therefore **0.7–1.1s of decode** — over the 500ms target. Realistic mitigations, in order of cheapness: keep the model resident (load time otherwise dominates), reuse the KV-cache for the static system prompt, drop to Qwen3-1.7B/Llama-3.2-1B (~2× decode speed) for the default path, and treat 4B as the "quality" toggle. UNVERIFIED: exact tok/s for Qwen3-4B-Instruct-2507 specifically on M5 Max — the 135–148 figure is the 4B class on that chip.\n- Instruction-following: Qwen3\'s instruct line is the community default for exactly this transform task, and the `-2507` instruct variant avoids the thinking-mode latency trap (a reasoning model burning tokens "thinking" before your 200ms budget starts). [Superwhisper\'s own docs](https://superwhisper.com/docs/models/language) validate the shape: a dedicated small cleanup model, and their guidance warns that "every LLM will \'improve\' your output whether you want it to or not" — so **ship a no-LLM verbatim mode**, which is also your latency escape hatch. INFERRED (not benchmarked head-to-head here): Gemma-3-4B and Phi-4-mini are speed-comparable; Qwen\'s edge is formatting-instruction adherence, which you should confirm with a 20-prompt eval in week one — it\'s a half-day task and the swap is one string.\n\n**3. Architecture — Swift menubar + Python MLX sidecar is the right call, and the pure-Swift MLX route is evidentially dead.**\n- The decisive evidence: [FluidInference/swift-parakeet-mlx](https://github.com/FluidInference/swift-parakeet-mlx) — the one serious Swift-MLX Parakeet port — was **archived July 2025**; maintainers state performance "wasn\'t able to achieve the performance we wanted" and they moved to CoreML (FluidAudio). Its README also notes MLX-Swift builds **require full Xcode for Metal shader compilation** — and this machine has only Command Line Tools (verified: `xcode-select -p` → `/Library/Developer/CommandLineTools`). So pure-Swift MLX STT fails on both maturity and local buildability.\n- The sidecar pattern is proven in the wild: [osadalakmal/parakeet-dictation](https://github.com/osadalakmal/parakeet-dictation) and [qazi0/parakeet-mlx-transcribe](https://github.com/qazi0/parakeet-mlx-transcribe) do hotkey → parakeet-mlx → paste; [Superwhisper itself now runs Parakeet-TDT v3 and Qwen3-ASR "in-process via MLX"](https://voice-list.com/blog/superwhisper-which-llm-to-use/) in production. The polished Swift-native apps ([VoiceInk](https://github.com/Beingpax/VoiceInk), 4.4k★ GPL-3.0; [MacParakeet](https://github.com/moona3k/macparakeet), 449★ GPL-3.0, 97.9% Swift) use whisper.cpp/CoreML precisely because they *avoided* MLX — with MLX required, Python sidecar is the mature-API path (parakeet-mlx and mlx-lm are Python-first).\n- Mic capture: capture with **AVAudioEngine in Swift** (VoiceInk\'s approach — PCM buffers in memory), downsample to 16kHz mono Float32, and stream frames over a unix socket to the sidecar. Simpler projects write temp wav files; that\'s acceptable for v0 (adds ~tens of ms for a 30s utterance, ~1MB file) but the socket protocol is what `transcribe_stream` wants later, so define the framing (4-byte length prefix + PCM, JSON control messages) on day one. Do **not** capture the mic in Python — that would move the microphone TCC prompt onto an unbundled python binary, which is fragile.\n- Toolchain reality check (verified locally): Swift 6.3.3 targets `arm64-apple-macosx26.0` and SwiftPM works without Xcode; system Python is 3.9.6 (too old for MLX, needs ≥3.10) but **uv 0.11.27 is installed**, so `uv python install 3.12 && uv sync` bootstraps the sidecar in minutes. Effort estimate for the batch-mode MVP (hotkey → record → transcribe → paste): **2–4 days**. Real distribution cost: SwiftPM gives you a bare executable — you hand-assemble the .app bundle (Info.plist with `LSUIElement`, `NSMicrophoneUsageDescription`) via a build script, and you bundle-or-bootstrap the Python env (uv-on-first-run is the pragmatic open-source answer; PyInstaller+MLX is notoriously finicky).\n\n**4. Text insertion — what shipping dictation apps actually do is pasteboard + synthesized Cmd-V, not AX setValue.**\n- Primary source, read directly from [VoiceInk\'s `CursorPaster.swift`](https://github.com/Beingpax/VoiceInk/blob/main/VoiceInk/Paste/CursorPaster.swift): snapshot clipboard → set text → post **CGEvent Cmd-V** (`virtualKey 0x37`/`0x09`, `.cghidEventTap`, gated on `AXIsProcessTrusted()`) → restore clipboard after ~250ms, with an AppleScript `System Events keystroke` variant as a user-selectable fallback (their comments even handle QWERTY-⌘ layout remapping via `key code 9`). Per-character CGEvent typing is too slow/unreliable for paragraphs; AX `AXUIElement` setValue fails in non-AX-compliant apps (Electron, some browsers) so nobody ships it as the primary path.\n- TCC permissions, concretely: **Microphone** (AVAudioEngine, Info.plist usage string), **Accessibility** (required for posting CGEvents — `AXIsProcessTrusted`), and **Input Monitoring** if you listen for the global hotkey via `CGEventTap` (`CGRequestListenEventAccess`). Note for macOS 26 specifically: [`NSEvent.addGlobalMonitorForEvents` reportedly crashes on macOS 26](https://developer.apple.com/forums/thread/819555) (Bus error in GlobalObserverHandler), pushing you to CGEventTap — or use `RegisterEventHotKey` (Carbon), which needs *no* TCC permission for simple hotkeys and is the cheapest v0 choice. Execution trap with real schedule impact: **TCC tracks permission grants by code-signing identity — ad-hoc-signed rebuilds reset your Accessibility grant every build.** Get a stable signing identity (free Apple ID development cert suffices) in week one or you\'ll re-grant permissions dozens of times a day.\n\n## Strongest point from this lens\n\nThe MVP is days, not weeks, and nothing about the hard latency target requires the hard engineering. Parakeet TDT on MLX is so much faster than real time (~0.5s for a full sample; 80ms-class for short utterances) that **batch-on-release already beats the <1s goal for 30s utterances** — streaming, LLM cleanup, and word-timestamp UX are all incremental upgrades on a working product. The only decisions you can\'t cheaply walk back are the Swift-shell/Python-sidecar split, the socket protocol, and the license; everything else (STT model, cleanup model, even mlx-whisper vs parakeet) is a config swap. Sequence accordingly: sign stably, define the IPC framing, pick the license — then iterate freely.\n\n## Confidence, and what would change my view\n\n**High confidence** on: parakeet-mlx over mlx-whisper for latency (two independent benchmark sources + streaming API existence verified in-repo); pasteboard+Cmd-V as the shipping insertion pattern (read the actual VoiceInk source); Python sidecar over Swift-MLX (the archived swift-parakeet-mlx repo is direct evidence, and the missing-Xcode constraint is verified locally). **Medium confidence** on: Qwen3-4B as the cleanup pick (class-level tok/s data, but no head-to-head formatting-adherence benchmark — I\'d run the 20-prompt eval before committing) and the exact <500ms feasibility (decode math says it needs a smaller model or short outputs). What would change my view: a maintained, benchmarked MLX-Swift streaming STT appearing (it would collapse the sidecar); evidence that parakeet-mlx\'s WER on *your* dictation audio is materially worse than mlx-whisper\'s (the 6.3%-vs-2.5% figure came from a competitor\'s internal test — re-measure); or Qwen3-ASR-via-MLX maturing enough to beat Parakeet on accuracy at similar speed.\n\n## Open questions this lens raises\n\n1. **Sidecar distribution:** uv-bootstrap on first launch vs bundling a relocatable Python — the former is a 1-day task but adds a network-on-first-run dependency to a "local-first" app. Needs a product call.\n2. **Actual parakeet-mlx WER on the target user\'s voice/domain** — no trustworthy public per-sample-length WER for the MLX port specifically; a 1-hour self-recorded eval settles it.\n3. **Prompt-cache TTFT for the cleanup LLM in mlx-lm** — whether cached system-prompt TTFT plus ~50 tokens of output fits 500ms on M5 Max is measurable in an afternoon and decides 4B-default vs 1.7B-default.\n4. **macOS 26 CGEventTap behavior** — the NSEvent global-monitor crash report suggests macOS 26 changed event-monitoring internals; verify RegisterEventHotKey and CGEventTap on 26.5.1 (this machine) before building the hotkey layer around either.\n\n**Verified:** local toolchain facts (`swift 6.3.3`, CLT-only, Python 3.9.6, uv 0.11.27, 38.6GB M5 Max, macOS 26.5.1 — command output above); VoiceInk paste implementation (source quoted); parakeet-mlx streaming API/timestamps/license (repo fetched); swift-parakeet-mlx archived status (repo fetched); lightning-whisper-mlx last release April 2024 (PyPI). **Inferred:** Qwen3-4B instruction-following edge over Gemma/Phi (community reputation, no head-to-head eval here); 0.7–1.1s decode estimate for paragraph cleanup (arithmetic from class-level tok/s); wav-handoff latency cost (file-size arithmetic, not measured).\n\nSources: [dicta.to Whisper vs Parakeet](https://dicta.to/blog/whisper-vs-parakeet-vs-apple-speech-engine/) · [dicta.to 13k-recording comparison](https://dicta.to/blog/speech-to-text-engine-comparison-mac-2026/) · [senstella/parakeet-mlx](https://github.com/senstella/parakeet-mlx) · [parakeet-mlx PyPI](https://pypi.org/project/parakeet-mlx/) · [FluidInference/swift-parakeet-mlx](https://github.com/FluidInference/swift-parakeet-mlx) · [Beingpax/VoiceInk](https://github.com/Beingpax/VoiceInk) · [moona3k/macparakeet](https://github.com/moona3k/macparakeet) · [LLMCheck benchmarks](https://llmcheck.net/benchmarks) · [lightning-whisper-mlx PyPI](https://pypi.org/project/lightning-whisper-mlx/) · [Blaizzy/mlx-audio](https://github.com/Blaizzy/mlx-audio) · [Superwhisper language-model docs](https://superwhisper.com/docs/models/language) · [voice-list.com Superwhisper LLM guide](https://voice-list.com/blog/superwhisper-which-llm-to-use/) · [Apple Developer Forums — real-time voice input thread](https://developer.apple.com/forums/thread/819555) · [osadalakmal/parakeet-dictation](https://github.com/osadalakmal/parakeet-dictation) · [qazi0/parakeet-mlx-transcribe](https://github.com/qazi0/parakeet-mlx-transcribe)\n', 'stderr': '', 'stdout_truncated': False, 'stderr_truncated': False, 'lens': 'pragmatist'}
