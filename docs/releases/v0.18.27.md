# Velora 0.18.27

Action results now open the exact window you asked for.

- Background Actions still leave the app you are using alone.
- Clicking a result card now restores a hidden target window instead of only
  activating its app.
- If macOS exposes a different app copy or window, Velora stops instead of
  opening the wrong one.
